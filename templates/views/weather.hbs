<!DOCTYPE html>
<html lang="en">

<head>
   {{>headerlinks}}
</head>

<body>
    {{>navbar}}

    
    <div class="container-fluid main_header">
        <div class="row">
            <div class="col-md-10 col-12 mx-auto">
               <div class="main_content">
                <form class="temp_form">
                    <input type="text"id="cityName" placeholder="Enter your city name" autocomplete="off">
                    <input type="submit" value="search" id= "submitBtn">
                </form>
               </div>
               <div class="temp_Info">
                <div class="top_layer">
                    <p id="day">Saturday</p>
                    <p id="today_date">1 APRIL</p>
                </div>
                <div class="main_layer">
                    <p id="city_name">Get Output Here</p>
                    <div class="middle_layer ">
                        <p id="temp"><span>0</span><sup>0</sup>C</p>
                        <p id="temp_status"><i class="fa fa-solid fa-cloud"></i></p>
                    </div>

                </div>
               </div>
            </div>
        </div>
    </div>


{{>footer}}
<script src="js/main.js"></script>
<script>
    const day = document.getElementById('day')
    const today_date = document.getElementById('today_date')
    const getCurrentDay = () => {
        let weekday = new Array(7);
        weekday[0]="Sunday"
        weekday[1]="Monday"
        weekday[2]="Tuesday"
        weekday[3]="Wedday"
        weekday[4]="Thursday"
        weekday[5]="Friday"
        weekday[6]="Saturday"

        let currentTime = new Date();
        let day= weekday[currentTime.getDay()]
        return day
    }
    const getCurrentMonth = ()=>{
        let month = new Array(12);
        month[0]="Jan"
        month[1]="Feb"
        month[2]="March"
        month[3]="April"
        month[4]="May"
        month[5]="June"
        month[6]="July"
        month[7]="August"
        month[8]="September"
        month[9]="October"
        month[10]="November"
        month[11]="December"

        let current = new Date()
        let m = current.getMonth()
        let mon = month[m]
        let datee = current.getDate()
        return `${datee}, ${mon}`

    }
    
    day.innerHTML = getCurrentDay()
    today_date.innerHTML = getCurrentMonth()

</script>
</body>
</html>